import { NextApiRequest, NextApiResponse } from "next";

export default async function seederCandidateValuePost (req: NextApiRequest, res: NextApiResponse) {
    if(req.method === "POST"){
        const body = req.body
        
    }
}